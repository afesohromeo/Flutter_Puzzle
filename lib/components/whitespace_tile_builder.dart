import 'package:flutter/material.dart';

class WhiteSpaceTileBuilder extends StatelessWidget {
  const WhiteSpaceTileBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
